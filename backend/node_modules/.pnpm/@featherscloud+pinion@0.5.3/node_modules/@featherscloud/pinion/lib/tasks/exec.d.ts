import { Callable, PinionContext } from '../core.js';
/**
 * Run a command with arguments.
 *
 * @param cmd  The command to run
 * @param _args The arguments for the command
 * @returns The current context
 */
export declare const exec: <C extends PinionContext>(cmd: Callable<string, C>, _args?: Callable<string[], C>) => (ctx: C) => Promise<C>;
