import { PinionContext, Callable } from '../core.js';
import { WriteFileOptions } from './helpers.js';
/**
 * Renders a template to a file.
 *
 * @param template The template to render
 * @param target The target file handle, usually provided by `to()`
 * @returns The generator context
 */
export declare const renderTemplate: <C extends PinionContext>(template: Callable<string, C>, target: Callable<string, C>, options?: Partial<WriteFileOptions>) => (ctx: C) => Promise<C>;
